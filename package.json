{
	"name": "dumb-query-selector",
	"version": "3.2.0",
	"description": "Shortcuts to the querySelector(All) functions, the (All) instead returning an array of nodes (because NodeLists are dumb)",
	"author": "Emanuel Rabina <emanuelrabina@gmail.com> (http://www.ultraq.net.nz/)",
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/ultraq/dumb-query-selector.git"
	},
	"main": "dumb-query-selector.js",
	"module": "src/dumb-query-selector.js",
	"scripts": {
		"lint": "eslint src test",
		"build": "babel src/dumb-query-selector.js --out-file dumb-query-selector.js",
		"test": "npm run lint --silent && nyc mocha --require babel-register",
		"coverage": "nyc report --reporter=text-lcov | coveralls",
		"prepublish": "npm run build"
	},
	"devDependencies": {
		"babel-cli": "^6.24.1",
		"babel-preset-es2015": "^6.24.1",
		"babel-register": "^6.24.1",
		"chai": "^4.0.2",
		"coveralls": "^2.13.1",
		"eslint": "^4.1.1",
		"eslint-config-ultraq": "^2.0.0",
		"jsdom": "^9.9.0",
		"mocha": "^3.2.0",
		"nyc": "^10.3.2"
	}
}
